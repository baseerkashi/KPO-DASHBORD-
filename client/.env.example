# Optional: production API base URL (no trailing slash). Dev uses Vite proxy → /api
# VITE_API_URL=https://your-backend.example.com

# Dev only: backend port for Vite proxy (must match server PORT in server/.env)
# VITE_API_PORT=5001
